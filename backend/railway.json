{
  "root": "backend",
  "build": {
    "builder": "nixpacks",
    "nixpacks": {
      "plan": {
        "packages": [
          "php",
          "php-cgi",
          "phpPackages.mbstring",
          "phpPackages.iconv",
          "phpPackages.mongodb"
        ],
        "commands": [
          "sh /app/backend/install.sh"
        ]
      }
    }
  },
  "start": "php -S 0.0.0.0:$PORT -t backend"
}
